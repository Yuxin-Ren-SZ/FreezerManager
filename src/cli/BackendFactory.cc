// SPDX-License-Identifier: AGPL-3.0-or-later

#include "cli/BackendFactory.h"

// Intentionally empty: cli/BackendFactory.h re-exports the shared storage backend
// factory. This translation unit stays in the CLI target so existing build files
// and IDE project structure do not need to special-case the migration.
